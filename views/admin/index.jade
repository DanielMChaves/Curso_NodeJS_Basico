extends ../layout.jade
block contenido
	h2 Panel de Administración
	table(class="table")
		caption Productos Actuales
		thead
			tr
				th #
				th Nombre
				th Descripcion
				th Precio
				th Acciones
		tbody
			each product in products
				tr
					td= product.id
					td= product.title
					td= product.description
					td= product.price
					td
						a(href="/menu/edit/#{product.id}") Editar 
						=" - "
						a(href="/menu/delete/#{product.id}" class="btn") Eliminar 